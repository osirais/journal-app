import { AchievementsMap } from "@/components/achievements-map";

export const metadata = {
  title: "Achievements"
};

const Page = () => {
  return (
    <div>
      <AchievementsMap />
    </div>
  );
};

export default Page;
